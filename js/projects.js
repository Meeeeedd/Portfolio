// js/projects.js - Projects Page Specific Scripts

document.addEventListener('DOMContentLoaded', () => {
    // Any future interactivity unique to the Projects page would go here.
    // For example, filtering projects, lazy loading, etc.
    // The global scroll animation logic for .animate-on-scroll is handled by main.js
});
